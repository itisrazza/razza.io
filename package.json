{
  "name": "razza.io",
  "private": true,
  "scripts": {
    "serve": "tsc --watch & bundle exec jekyll serve",
    "build": "tsc && bundle exec jekyll build",
    "page:new": "node _tasks/newPage.js"
  },
  "license": "MIT",
  "devDependencies": {
    "commander": "^11.0.0",
    "slugify": "^1.6.6",
    "typescript": "^5.1.3",
    "yaml": "^2.3.1"
  }
}
